[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "edterm-rust-ext"
version = "0.1.0"
description = "Optional Rust accelerator for edterm EDR parsing"
requires-python = ">=3.8"
